package com.projectmodule.work.domain;

/** Fields a Risk list may be sorted by, per {@code docs/project/15-LIST-SPEC.md} §2. */
public enum RiskSortField {
    CREATED_AT,
    NAME,
    PRIORITY,
    STATUS
}
