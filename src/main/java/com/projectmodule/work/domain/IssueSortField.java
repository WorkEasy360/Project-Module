package com.projectmodule.work.domain;

/** Fields an Issue list may be sorted by, per {@code docs/project/15-LIST-SPEC.md} §2. */
public enum IssueSortField {
    CREATED_AT,
    NAME,
    PRIORITY
}
