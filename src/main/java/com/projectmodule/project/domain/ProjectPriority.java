package com.projectmodule.project.domain;

/** Relative importance of a project, used for ordering and for surfacing attention. */
public enum ProjectPriority {

    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
